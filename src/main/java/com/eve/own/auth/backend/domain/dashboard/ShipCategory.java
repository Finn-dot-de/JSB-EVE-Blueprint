package com.eve.own.auth.backend.domain.dashboard;

/** Die Kaesten, in denen das Dashboard die Bestaende eines Accounts zeigt. */
public enum ShipCategory {
    SUBCAPITAL,
    CAPITAL,
    INDUSTRIAL,
    NOTABLE,
    STRUCTURES
}
